# Port targets — `ts-rs-kotlin` (driven by codex-rs demand)

> Generated by `scaffold/generate_port_targets.py` from `codex_external_usage.json`. Do not hand-edit. Re-run after rebuilding the usage data.

This is the **minimum subset** of `ts-rs` that codex-rs uses, and therefore the minimum subset that `ts-rs-kotlin` needs to port in order for codex-kotlin to compile and run. Symbols are sorted by codex demand (most-used first). Anything not listed here is not currently required by codex — defer it.

**Jira Initiative**: [COD-173] (rank `54`, tier `direct`, status `To Do`)

**Cargo package name**: `ts-rs`
**Rust module name (in `use` statements)**: `ts_rs`

## Summary

- **2** distinct symbols imported by codex-rs
- **29** total `use` statements
- **28** codex source files affected
- **5** codex member crates transitively depend on this

## Granularity caveat

This list is **import-level**, not call-site-level. A symbol appearing here means codex `use`'d it; the specific functions, methods, fields, or trait impls codex calls on the imported name are not enumerated. For an initial port, implement the symbol's full API as defined in upstream Rust. For a tighter minimum, drill into each codex source file listed below and grep for the imported name to find concrete invocations — the graph tells you exactly which files to grep.

## Symbols by codex demand (port these in order)

### `ts_rs::TS`

- **28 `use` site(s)** across **5 codex member crate(s)**: `codex-app-server-protocol`, `codex-exec`, `codex-git-utils`, `codex-protocol`, `codex-utils-absolute-path`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-protocol/src/export.rs:37` (`codex-app-server-protocol`) — `use ts_rs::TS`
- `app-server-protocol/src/jsonrpc_lite.rs:9` (`codex-app-server-protocol`) — `use ts_rs::TS`
- `app-server-protocol/src/protocol/common.rs:16` (`codex-app-server-protocol`) — `use ts_rs::TS`
- `app-server-protocol/src/protocol/v1.rs:22` (`codex-app-server-protocol`) — `use ts_rs::TS`
- `app-server-protocol/src/protocol/v2.rs:124` (`codex-app-server-protocol`) — `use ts_rs::TS`
- `app-server-protocol/src/schema_fixtures.rs:21` (`codex-app-server-protocol`) — `use ts_rs::TS`
- `exec/src/exec_events.rs:6` (`codex-exec`) — `use ts_rs::TS`
- `git-utils/src/info.rs:16` (`codex-git-utils`) — `use ts_rs::TS`
- `protocol/src/account.rs:4` (`codex-protocol`) — `use ts_rs::TS`
- `protocol/src/agent_path.rs:7` (`codex-protocol`) — `use ts_rs::TS`
- `protocol/src/approvals.rs:15` (`codex-protocol`) — `use ts_rs::TS`
- `protocol/src/config_types.rs:16` (`codex-protocol`) — `use ts_rs::TS`
- `protocol/src/dynamic_tools.rs:6` (`codex-protocol`) — `use ts_rs::TS`
- `protocol/src/items.rs:35` (`codex-protocol`) — `use ts_rs::TS`
- `protocol/src/mcp.rs:9` (`codex-protocol`) — `use ts_rs::TS`
- `protocol/src/memory_citation.rs:4` (`codex-protocol`) — `use ts_rs::TS`
- `protocol/src/message_history.rs:4` (`codex-protocol`) — `use ts_rs::TS`
- `protocol/src/models.rs:12` (`codex-protocol`) — `use ts_rs::TS`
- `protocol/src/openai_models.rs:16` (`codex-protocol`) — `use ts_rs::TS`
- `protocol/src/parse_command.rs:5` (`codex-protocol`) — `use ts_rs::TS`
- _… and 8 more (see `codex_external_usage.json` for the full list)._

### `ts_rs::TypeVisitor`

- **1 `use` site(s)** across **1 codex member crate(s)**: `codex-app-server-protocol`

Codex source locations (relative to `tmp/codex/codex-rs/`):

- `app-server-protocol/src/schema_fixtures.rs:22` (`codex-app-server-protocol`) — `use ts_rs::TypeVisitor`
